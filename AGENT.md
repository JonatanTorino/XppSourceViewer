Las instrucciones de este repositorio están en **[CLAUDE.md](CLAUDE.md)**.

Este archivo existe para las herramientas que buscan `AGENT.md`. Debería ser un
symlink a `CLAUDE.md`, pero crear symlinks en Windows exige privilegios de
administrador, así que quedó como puntero.

La regla que no hay que perderse si solo se lee este archivo: **no hacer un
commit ni abrir un PR por cada pedido.** El trabajo se acumula, se reporta qué
cambió, se recomienda dónde cortar, y el corte lo decide Jonatan.
